
spring:
  redis:
    host: 123.60.95.208
    port: 6379

    lettuce:
      shutdown-timeout: 0
  mvc:
    # URL响应地址（Springboot默认为/**)
    static-path-pattern: /file/**
  web:
    resources:
      # 静态文件地址，保留官方内容后，进行追加
      static-locations: classpath:/static,classpath:/public,classpath:/resources,classpath:/META-INF/resources,file:file
